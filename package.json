{
  "name": "monorepo-sites",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/generate.mjs",
    "add:client": "node scripts/add-client.mjs",
    "dev": "npx live-server dist --port=5500 --mount=/:dist"
  },
  "devDependencies": {
    "ejs": "^3.1.10",
    "fs-extra": "^11.2.0",
    "glob": "^11.0.0",
    "html-minifier-terser": "^7.2.0",
    "live-server": "^1.2.2"
  }
}